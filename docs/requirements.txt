# Zero-Order Optimization (ZO) Training Requirements
# 零次优化训练项目依赖包

# ============================================================================
# 核心深度学习框架
# ============================================================================
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0

# ============================================================================
# 数据处理和可视化
# ============================================================================
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ============================================================================
# 工具库
# ============================================================================
tqdm>=4.65.0

# ============================================================================
# 联邦学习（可选，仅 flwr_server.py 需要）
# ============================================================================
flwr>=1.4.0

# ============================================================================
# 说明
# ============================================================================
# 1. PyTorch 版本建议根据 CUDA 版本选择：
#    - CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#    - CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#                  (CUDA 12.1最低版本2.1.0，推荐2.1.2)
#    - CPU only: pip install torch torchvision torchaudio
#
# 2. 安装命令：
#    pip install -r requirements.txt
#
# 3. 如果只需要核心训练功能（不使用联邦学习和高级可视化），
#    可以只安装以下包：
#    - torch
#    - transformers
#    - datasets
#    - tqdm
#    - matplotlib
#
# 4. 联邦学习功能（flwr_server.py）需要额外安装 flwr
# 5. 高级可视化（plot_all_results.py）需要 seaborn

